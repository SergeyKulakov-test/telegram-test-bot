{
  "name": "telegram-test-prep-bot",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "install-deps": "npm install",
    "build": "npm run install-deps && tsc && node copy-files.js",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "telegraf": "^4.16.3",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": "18.x"
  }
}